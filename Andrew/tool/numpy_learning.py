import numpy as np

# ==============================
# 1. 创建数组
# ==============================
X = np.array([[1,2],[3,4],[5,6]])
y = np.array([1,0,1])


# ==============================
# 2. zeros / ones 初始化
# (常用于初始化模型参数)
# ==============================
w = np.zeros((2,1))
b = np.zeros((1))


# ==============================
# 3. random 初始化
# (深度学习常见)
# ==============================
W = np.random.randn(2,2)


# ==============================
# 4. shape 查看维度
# ==============================
print(X.shape)


# ==============================
# 5. reshape 改变形状
# ==============================
y = y.reshape(3,1)


# ==============================
# 6. transpose 转置
# ==============================
XT = X.T


# ==============================
# 7. 矩阵乘法
# (机器学习核心操作)
# ==============================
z = np.dot(X, w)

# 或
z = X @ w


# ==============================
# 8. 向量化运算
# (避免 python for loop)
# ==============================
X = X + 1
X = X * 2


# ==============================
# 9. broadcasting
# ==============================
z = X @ w + b


# ==============================
# 10. sum 求和
# (常用于loss计算)
# ==============================
loss = np.sum(z)


# ==============================
# 11. mean 平均值
# ==============================
loss = np.mean(z)


# ==============================
# 12. exp / log
# (逻辑回归、softmax)
# ==============================
sigmoid = 1 / (1 + np.exp(-z))
log_val = np.log(sigmoid)


# ==============================
# 13. where 条件筛选
# ==============================
pred = np.where(sigmoid > 0.5, 1, 0)


# ==============================
# 14. argmax
# (分类问题)
# ==============================
probs = np.array([[0.1,0.7,0.2]])
label = np.argmax(probs)


# ==============================
# 15. linalg.norm
# (正则化 L2)
# ==============================
reg = np.linalg.norm(w)